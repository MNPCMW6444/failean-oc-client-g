import { AppName } from "@failean/shared-types";

const name: AppName = {
  low: "failean",
  up: "Failean",
  special: "fAIlean",
  allUp: "FAILEAN",
  //more after type...
};

// package.name
// index.html.head.meta.description
// index.html.head.title
// manifest.short_name
// manifest.name

export default name;
